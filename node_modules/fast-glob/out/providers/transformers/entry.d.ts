import Settings from '.js';
import { EntryTransformerFunction } from '.js';
export default class EntryTransformer {
    private readonly _settings;
    constructor(_settings: Settings);
    getTransformer(): EntryTransformerFunction;
    private _transform;
}
